Archived ES 1m strategy sketches.

These files are kept out of the active `user_data/strategies/*.py` discovery
surface because the default v0.4.1 Auto-Quant config is a 1h Binance crypto
portfolio and the agent contract caps active non-underscore strategy files at
three. Move one back to the parent strategies directory only inside a lane with
matching ES 1m data/config and after making room under the active cap.
